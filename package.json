{
  "name": "app",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "npm run build:vendor",
    "build:vendor": "esbuild compromise --bundle --format=esm --outfile=frontend/vendor/compromise.js && esbuild fuse.js --bundle --format=esm --outfile=frontend/vendor/fuse.js && esbuild nearley --bundle --format=esm --outfile=frontend/vendor/nearley.js",
    "build:css": "npx @tailwindcss/cli -i ./frontend/css/input.css -o ./frontend/css/style.css",
    "build:parser": "nearleyc frontend/utils/grammar.ne -o frontend/utils/grammar.js && node -e \"const fs = require('fs'); let c = fs.readFileSync('frontend/utils/grammar.js', 'utf8'); c = '/* eslint-disable */\\n' + c.replace('(function () {', 'export const grammar = (function () {').replace(/if \\(typeof module[\\s\\S]*?window\\.grammar = grammar;\\s*\\}/, 'return grammar;').replace('function id(x) { return x[0]; }', ''); fs.writeFileSync('frontend/utils/grammar.js', c);\"",
    "test": "jest",
    "js-checks": "./tests/run-js-checks.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ttbt-io/skorekeeper.git"
  },
  "type": "module",
  "devDependencies": {
    "@babel/core": "^7.26.9",
    "@babel/preset-env": "^7.26.9",
    "@stylistic/eslint-plugin": "^4.2.0",
    "babel-jest": "^30.0.0-alpha.3",
    "esbuild": "^0.28.0",
    "eslint": "^9.21.0",
    "globals": "^16.0.0",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "tailwindcss": "^4.1.17"
  },
  "dependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "autoprefixer": "^10.4.22",
    "compromise": "^14.14.3",
    "fuse.js": "^7.1.0",
    "nearley": "^2.20.1",
    "postcss": "^8.5.6"
  }
}
