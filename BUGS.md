# BUGS

* Add tests to verify that the "sync" button on the dashboard doesn't bypass the conflict detection.
  * The test will most likely fail.
  * I think we'll need to add the last action ID to GameMetadata.
